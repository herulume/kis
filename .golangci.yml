run:
  build-tags:
    - integration

linters-settings:
  goimports:
    # Separate internal packages from external
    local-prefixes: github.com/herulume/kis

linters:
  # Enable extra linters besides the default ones
  enable:
    - goimports
    - godot
    - goconst
    - gofumpt
    - govet
    - stylecheck
    - unconvert
    - unparam
    - misspell
    - whitespace
    - bodyclose
